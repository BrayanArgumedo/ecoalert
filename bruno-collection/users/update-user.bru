meta {
  name: Update User
  type: http
  seq: 3
}

patch {
  url: {{base_url}}/users/{{user_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "nombre": "Pepe Francisco Actualizado",
    "localidad": "Barrio Nuevo"
  }
}
